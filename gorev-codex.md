Önce AJAN-KURALI.md ve GOREV.md oku (HAFIZA.md son 60 satır: aşı takvimi ve ilaç modülü notları). GOREV.md "Adaylar" 8, 10, 5, 3 numaralı maddeleri BU SIRAYLA uygula; her madde ayrı commit ([codex] ... İngilizce mesaj, gövdesinde 1-2 satır neden). Başlamadan ve her commit'ten önce: cd app; npm test ve npx tsc -b --noEmit yeşil.

Madde 8 — app/src/features/calendar/schedule.ts `buildSchedule`: TAKVİMİ DEĞİŞTİRME. Mevcut davranışı sabitleyen regresyon testleri yaz (app/src/__tests__/schedule.test.ts): doğum tarihi verildiğinde üretilen kayıtların (a) anahtarları benzersiz, (b) tarihler artan/doğru ay ofsetinde (ör. 2. ay, 4. ay, 6. ay, 9. ay KKK ek doz, 12. ay, 18. ay, 48. ay Suçiçeği 2 — koddaki listeye göre), (c) ay sonu doğum (31 Ocak → 28/29 Şubat) ve artık yıl (29 Şubat doğum) sınırları, (d) izlem (15. gün, 41. gün vb.) kayıtları. Beklenen değerleri koddan türet ama sabit literal olarak yaz (snapshot değil, açık tarih).

Madde 10 — Yaş hesabı (doğum tarihini parseISO ile ayrıştırıp gün farkı / 30.4375) 4 yerde tekrar ediyor: Growth.tsx, MilestoneCard.tsx, StatusPanel.tsx, Assessment.tsx. app/src/lib/age.ts içine `ageMonths(birthISO: string, now: number): number` ve gerekiyorsa `ageDays` yaz; MEVCUT DAVRANIŞI birebir koru (aynı formül, aynı yuvarlama). 4 yeri buna geçir. Test: sınır tarihler (aynı gün → 0, 30 gün, 365 gün, geçersiz tarih davranışı mevcut neyse o).

Madde 5 — İlaç kür doz sayısı son 200 genel olaydan hesaplanıyor (MedsBlock.tsx:63, QuickLog.tsx:40). İlgili medId için Dexie'de ayrı sorgu (events where type=="ilac" and medId==X ya da mevcut şemadaki alan adı neyse) kullan; 200'den fazla başka olay araya girse de sayım doğru olsun. Saf sayım fonksiyonunu ayır ve vitest ile test et (Dexie'ye bağımlı değilse fake-indexeddb kullanma; saf fonksiyon yeter).

Madde 3 — app/src/features/notify/reminders.ts:88: abonelik yokken msgId "" ile yazılan yerel hatırlatma, abonelik açılınca aynı zamanlı olduğu için atlanıyor ve sunucuda zamanlanmıyor. Düzelt: abonelik varken msgId boş olan kayıtlar "sunucuda zamanlanmalı" sayılsın. En küçük değişiklik; saf karar fonksiyonunu ayırıp test et (aboneliksiz → abonelikli geçiş senaryosu). api/ tarafına dokunma.

Bitince GOREV.md: 8, 10, 5, 3'ü "Tamamlananlar"a taşı (hash), "Adaylar"dan sil, "Sıradaki adım"ı güncelle (kalan: 1, 6, 7 — sağlık eşiği, kaynak bekliyor), "Son güncelleyen: codex, 2026-09-20". Son mesaj ≤6 satır: test/tsc son satırı, 4 commit hash'i, dokunulan dosyalar, varsa çekince. Deploy, npm install, GitHub push yok.
