Önce AJAN-KURALI.md ve GOREV.md oku. Üç madde, BU SIRAYLA, her biri ayrı commit ([codex] ... İngilizce; gövdede neden). Her commit'ten önce: cd app; npm test ve npx tsc -b --noEmit yeşil. Sağlık eşiği İCAT ETME — aşağıdaki kararlar Claude (yönetici) tarafından verildi, aynen uygula.

Madde 7 — app/src/features/stats/Assessment.tsx kilo değerlendirmesi (yaklaşık satır 92-125). İki hata:
 (a) Dal sırası: `lastAge >= 14 && loss > 0` (14. günde hâlâ doğum kilosunun altında → "dikkat") dalı, `loss > 0.07` ("izle") dalının ARKASINDA kalıyor; 14. günde %8 kayıp yanlışlıkla "izle" oluyor. Sıra şu olacak: önce `loss > 0.10` → dikkat (mevcut mesaj); sonra `lastAge >= 14 && loss > 0` → dikkat ("14. günde hâlâ doğum kilosunun altında — hekime söyle"); sonra `loss > 0.07` → izle; sonra iyi.
 (b) Pencere: kontrol yalnız `lastAge <= 14` içinde; 15. günden sonra hâlâ doğum kilosunun altındaysa hiç uyarı yok. Değiştir: doğum kilosu varsa ve son ölçüm doğum kilosunun altındaysa ve lastAge >= 14 → her zaman "dikkat" (aynı mesaj, gün sayısı ile). lastAge < 14 için mevcut kayıp yüzdesi mantığı kalır. Kaynak metni (AAP_WEIGHT) değişmez.
 Değerlendirme mantığını saf bir fonksiyona çıkar (ör. app/src/features/stats/weightAssessment.ts: girdi doğum kilosu g, son kilo g, son ölçüm günü; çıktı status+note) ve tablo testi yaz: gün 3 %5 → iyi; gün 5 %8 → izle; gün 5 %11 → dikkat; gün 14 %8 → dikkat; gün 14 %2 → dikkat; gün 14 +1% → iyi; gün 20 %3 → dikkat; gün 20 +3% → (bu dal değil, g/gün mantığı; fonksiyon "uygulanmaz" döndürsün).

Madde 1 — app/src/features/meds/meds.ts `tooEarly` (satır ~29) ve MedsBlock.tsx (~54). Karar: "gerekirse" (PRN) ilaçta aralık KESİN tabandır → erken doz kontrolü %100 (aralık dolmadan verilirse uyar); planlı kür ilaçlarında mevcut %85 toleransı kalır. meds şemasında PRN/kür ayrımı hangi alanla tutuluyorsa onu kullan (ör. `asNeeded`, `prn`, süre yoksa PRN — koda bak, uydurma). MedsBlock'taki metin davranışla tutarlı olsun ("en az X saat ara" PRN'de kesin; kürde "±%15 tolerans" gibi kısa not). Test: 4 saatlik PRN: 3 sa 30 dk → early:true, 4 sa 0 dk → early:false; 4 saatlik kür: 3 sa 30 dk → early:false, 3 sa 0 dk → early:true.

Madde 6 — Sarılık kontrol saatleri (app/src/features/jaundice/JaundiceCard.tsx ~29, app/src/db/types.ts ~47): doğum tarihi gece yarısı sayılıyor. Karar: bebek kaydına isteğe bağlı `birthTime` ("HH:mm", string, opsiyonel) alanı ekle (Dexie şema versiyonunu artırmak gerekiyorsa mevcut kalıba uy — HAFIZA.md'de şema notları var; gerekmiyorsa artırma). Bebek düzenleme formuna (Settings içindeki bebek formu) "Doğum saati (isteğe bağlı)" input'u. JaundiceCard'daki saat hesabı `birthTime` varsa onu kullansın; yoksa mevcut davranış (00:00) + kartta küçük not: "doğum saati girilmedi, ±12 sa". Saat hesabını saf fonksiyona al (ör. `birthInstant(birthISO, birthTime?)`) ve test et: saat verili/verisiz, gece yarısı sınırı.

Bitince GOREV.md: 7, 1, 6 → "Tamamlananlar" (hash + tek satır ne yapıldı), "Adaylar" boşalınca bölümü "(kalan yok)" yap, "Sıradaki adım": "kullanıcı test eder (iPhone): ilaç PRN uyarısı, sarılık saati, kilo değerlendirmesi", "Son güncelleyen: codex, 2026-09-20". Son mesaj ≤6 satır: test/tsc son satırı, 3 hash, dosyalar, çekince. Deploy, npm install, GitHub push yok.
